import { defineAppConfig } from 'nuxt/app';

export default defineAppConfig({
  primevueConfig: {
    // ripple: true,
    // inputStyle: 'outlined'
  },

  sakaiNuxt: {
    name: 'Sakai Nuxt'
  }
});
